{
  "name": "actour",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "(prettier \"client/**/*.{js, html}\" --write) && (prettier \"server/**/*.js\" --write)",
    "start": "cd server && npm install && npm run start",
    "build": "cd client && npm install && npm run build",
    "dev-serve": "cd server && npm install && npm run dev",
    "dev-start": "cd client && npm run start",
    "heroku-postbuild": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zackcarlson/actour.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/zackcarlson/actour/issues"
  },
  "homepage": "https://github.com/zackcarlson/actour#readme",
  "devDependencies": {},
  "dependencies": {
    "dotenv": "^8.2.0",
    "prettier": "^2.0.5",
    "react-apollo": "^3.1.5"
  }
}
