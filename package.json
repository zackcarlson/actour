{
  "name": "actour",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier \"client/**/*.{js, html}\" --write",
    "start": "cd server && npm install && npm run start",
    "build": "cd client && npm install && npm run build",
    "dev": "(cd server && npm install && npm run dev) && (cd client && npm run start)",
    "heroku-postbuild": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/zackcarlson/actour.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/zackcarlson/actour/issues"
  },
  "homepage": "https://github.com/zackcarlson/actour#readme",
  "devDependencies": {},
  "dependencies": {
    "dotenv": "^8.2.0"
  }
}
